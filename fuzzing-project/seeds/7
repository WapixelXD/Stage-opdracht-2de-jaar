- method: GET
  path: /functions
  parameters:
    ? - limit
      - Query
    : !Number 0
    ? - offset
      - Query
    : !Number 0
    ? - orderType
      - Query
    : !String ASC
    ? - search
      - Query
    : !String '[SEARCH]'
